export const metadata = {
  title: 'Insight Hub - Stories',
  description: 'Discover compelling stories and insights from our community',
}

export default function BlogLayout({ children }) {
  return (
    <>
      {children}
    </>
  )
}
